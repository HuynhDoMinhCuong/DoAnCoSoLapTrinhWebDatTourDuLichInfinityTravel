@using Microsoft.AspNet.Identity
@if (Request.IsAuthenticated)

/*
{
using (Html.BeginForm("LogOff", "Account", FormMethod.Post, new { id = "logoutForm", @class = "navbar-right" }))
{
@Html.AntiForgeryToken()

<ul class="nav navbar-nav navbar-right">
    <li>
        @Html.ActionLink("Hello " + User.Identity.GetUserName() + "!", "Index", "Manage", routeValues: null, htmlAttributes: new { title = "Manage" })
    </li>
    <li><a href="javascript:document.getElementById('logoutForm').submit()">Log off</a></li>
</ul>
}
}
*/

{
    using (Html.BeginForm("LogOff", "Account", FormMethod.Post, new { id = "logoutForm", @class = "navbar-right" }))
    {
        @Html.AntiForgeryToken()

        <ul class="nav navbar-nav navbar-right">
            <li class="dropdown">
                <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button"
                   aria-haspopup="true" aria-
                   expanded="false">
                    @User.Identity.GetUserName() <span class="caret"></span>
                </a>
                <ul class="dropdown-menu">

                    @*<li><a href="@Url.Action("Attending", "Courses")">Course I'm Going</a></li>
                      <li><a href="@Url.Action("Following", "Courses")">Lecture I'm Flowing</a></li>
                      <li><a href="@Url.Action("Mine", "Courses")">My Upcoming Courses</a></li>*@

                    <li><a href="@Url.Action("ListLoaiVanChuyen", "LoaiVanChuyen")"> Danh sách loại vận chuyển</a></li>
                    <li><a href="@Url.Action("ListTour", "Tour")"> Danh sách tour </a></li>
                    <li><a href="@Url.Action("ListChiTietDonTour", "ChiTietDonTour")"> Chi tiết đơn tour </a></li>

                    

                    <li><a href="@Url.Action("ListNguoiDung", "NguoiDung")"> Danh sách khách hàng </a></li>

                    <li role="separator" class="divider"></li>
                    <li><a href="javascript:document.getElementById('logoutForm').submit()"> Thoát </a></li>

                </ul>

            </li>

        </ul>
    }



}


else
{
    <ul class="nav navbar-nav navbar-right">
        <li>@Html.ActionLink("Đăng ký(QTV) ", "Register", "Account", routeValues: null, htmlAttributes: new { id = "registerLink" })</li>
        <li>@Html.ActionLink("Đăng nhập(QTV) ", "Login", "Account", routeValues: null, htmlAttributes: new { id = "loginLink" })</li>
    </ul>


}
